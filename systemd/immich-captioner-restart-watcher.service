[Unit]
Description=Restart immich_captioner whenever immich_server restarts
After=docker.service
Requires=docker.service

[Service]
Type=simple
ExecStart=/opt/captioner/immich-semantic-captioner/scripts/restart-on-immich-restart.sh
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
